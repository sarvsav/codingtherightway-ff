on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: sarvsav/go-feature-flag-lint-action@65bda3e20d0d53cfbd8abbf615f0fda5025b7153
        id: hello          
        with:
          who-to-greet: 'codingtherightway'
